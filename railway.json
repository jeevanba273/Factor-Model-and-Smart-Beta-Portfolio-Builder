{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "apt-get update && apt-get install -y git-lfs && git lfs install && git lfs pull && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "streamlit run app.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}